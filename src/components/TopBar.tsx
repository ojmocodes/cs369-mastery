// TopBar is now a minimal wrapper — main navigation is in Sidebar.
// This file kept for compatibility but the component is no longer rendered
// directly (App.tsx has its own top bar inline).

export {};
